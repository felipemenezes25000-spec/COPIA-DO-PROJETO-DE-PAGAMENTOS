{
  "buildCommand": "cd frontend-web && npm install && npm run build",
  "outputDirectory": "frontend-web/dist",
  "rewrites": [
    { "source": "/((?!assets/|_next/).*)", "destination": "/index.html" }
  ]
}
