# Scripts

Scripts auxiliares do repositório. Executar a partir da **raiz do repo**.

| Script | Descrição |
|--------|-----------|
| `perf-fix-all.ps1` | Aplica fixes de performance (mobile + backend). Uso: `.\scripts\perf-fix-all.ps1` ou `.\scripts\perf-fix-all.ps1 -DryRun` |

Scripts de infra (Terraform, schema): ver `infra/scripts/`.
